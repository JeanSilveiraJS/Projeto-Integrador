services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=gps-manager'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5433:5432'
